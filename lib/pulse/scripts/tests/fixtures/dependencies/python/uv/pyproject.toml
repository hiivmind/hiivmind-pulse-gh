[project]
name = "acme-api"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28,<3.0",
    "click==8.1.3",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0",
]

[dependency-groups]
dev = [
    "ruff>=0.5",
]
