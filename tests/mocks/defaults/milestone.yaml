# Default Mock Responses for Milestone Domain

mocks:
  # REST: List all milestones
  - pattern: "repos/.*/milestones.*state=all"
    type: "rest"
    fixture: "rest/milestone/list_all.json"

  # REST: List open milestones
  - pattern: "repos/.*/milestones.*state=open"
    type: "rest"
    fixture: "rest/milestone/list_all.json"

  # REST: List closed milestones
  - pattern: "repos/.*/milestones.*state=closed"
    type: "rest"
    fixture: "_synthetic/empty/empty_array.json"

  # REST: Get single milestone
  - pattern: "repos/.*/milestones/[0-9]+"
    type: "rest"
    fixture: "rest/milestone/get_single.json"
