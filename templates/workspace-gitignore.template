# hiivmind-pulse-gh workspace repo
# Per-machine transient state — never commit (two machines legitimately differ)
user.yaml
poll-state.yaml
project-snapshot.json
*-result.yaml
fleet-evidence.yaml
# Transient F4 materialize evidence (carries raw file content) — should live
# under a run-specific 0700 temp dir (dependency_evidence.secure_run_dir) and
# be deleted once F4 emits its content-free snapshot; never committed.
dependency-evidence.json
# Content-free F4 snapshot — transient, run-scoped; never committed.
deps-snapshot.json
log/
.assignments-tmp.json
.project-changes.json
runs/local/
